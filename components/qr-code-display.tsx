"use client"

// This component previously rendered a listing-level QR payload.
// Listing-level QRs were removed: QR tickets are now generated when a reservation is made.
// Keep a tiny stub so imports don't break during the transition.

export default function QRCodeDisplayStub() {
  return <div />
}
